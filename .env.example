# ATXP MCP Server Environment Variables

# Optional: ATXP Authentication Server URL (defaults to https://auth.atxp.ai)
ATXP_SERVER=https://auth.atxp.ai

# Development security setting - allows HTTP requests for local testing
# Set to "true" for development, "false" for production
ALLOW_INSECURE_HTTP_REQUESTS_DEV_ONLY_PLEASE=true

# ATXP Client Configuration
# Required: Connection string for ATXPAccount
# Format: https://accounts.atxp.ai?connection_token=[token]
# Example: ATXP_CONNECTION_STRING=https://accounts.atxp.ai?connection_token=FMgpVXHP7D4xK9vzrm74Y
ATXP_CONNECTION_STRING=

# Required: Destination wallet address for receiving payments
# This should be a valid Base network wallet address
# Example: FUNDING_DESTINATION=0x7F9D1a879750168b8f4A59734B1262D1778fDB5A
FUNDING_DESTINATION=

# Required: Network for payments (currently supports "base")
FUNDING_NETWORK=base

# Instructions:
# 1. Copy this file to .env
# 2. Set FUNDING_DESTINATION to your Base wallet address (for server)
# 3. Set ATXP_CONNECTION_STRING for client authentication and payments
# 4. For local development, these values will be used automatically
# 5. For production deployment, set environment variables in wrangler.jsonc or use:
#    wrangler secret put [VARIABLE_NAME] (if needed as a secret)